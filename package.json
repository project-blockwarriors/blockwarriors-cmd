{
  "name": "blockwarriors-cmd",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "BlockWarriors Command Block monorepo",
  "packageManager": "npm@10.9.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "codegen": "cd packages/backend && convex codegen",
    "deploy": "cd packages/backend && convex deploy"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "dependencies": {
    "uuid": "^13.0.0"
  }
}
